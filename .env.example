# Copy this file to .env for local Docker or local start.sh usage.
# Do not commit real secrets.
#
# The hosted UI does not require provider keys for local conversion.
# Enter provider credentials in Advanced options only when a cloud or
# vision/OCR feature needs them.

# Container UI bind settings.
MARKITDOWN_HOST=0.0.0.0
MARKITDOWN_PORT=8765
